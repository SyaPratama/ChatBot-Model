generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "postgresql"
    url = env("POSTGRES_PRISMA_URL") //use pooling connect
    directUrl = env("POSTGRES_URL_NON_POOLING") // not used pooling connect
}